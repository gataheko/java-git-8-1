# java-git-8-1
Arrays project that prompts the user to enter a month number (1–12) and displays the corresponding month name and sales figure using parallel arrays. Validates input, loops until the user quits, and prints the total yearly sales formatted as currency at the end. Built with Java in Eclipse.
